# SQLite implementations of the storage repositories.
# Mirror the function signatures of the _pg/ counterparts so the facade
# (app/db/repositories/<name>.py) can `import *` from whichever backend is active.
