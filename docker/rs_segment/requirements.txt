# Base image pytorch/pytorch:2.0.1-cuda11.7-cudnn8-runtime ships torch 2.0.1 +
# torchvision + CUDA 11.7. Only inference deps are added here. smp is pinned to
# 0.3.3 — the exact version that trained/pickled the checkpoint — so torch.load
# of the full model unpickles against matching class definitions.
segmentation-models-pytorch==0.3.3
timm==0.9.2
efficientnet-pytorch==0.7.1
pretrainedmodels==0.7.4
rasterio>=1.3.0,<1.5.0
numpy>=1.24.0,<2.0.0
Pillow>=9.5.0,<11.0.0
patchify==0.2.3
