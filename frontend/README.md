
# Agent-RS Frontend

Vite React frontend for the Agent-RS backend.

## Running the code

Run `npm install` to install dependencies.

Run `npm run dev` to start the development server.

The app defaults to `POST http://localhost:3000/api/chat`. Use the config panel
to provide a backend endpoint, provider base URL, API key, model, and system
prompt for local development.
  
